#!/bin/sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
bootstrap_python=${CDC_BOOTSTRAP_PYTHON:-python3}

"$bootstrap_python" "$repo_root/scripts/bootstrap.py" --quiet
exec "$repo_root/.codex/venv/bin/content-capture" --project "$repo_root/.codex/data" serve "$@"
